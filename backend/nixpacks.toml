[phases.setup]
nixPkgs = ["nodejs-18_x", "python310"]

[phases.install]
cmds = [
  "npm install",
  "pip3 install --no-cache-dir --timeout=300 -r requirements.txt || echo 'Python packages installation skipped due to timeout'"
]

[start]
cmd = "node server.js"

