[phases.setup]
nixPkgs = ["nodejs-18_x", "python310"]

[phases.install]
cmds = [
  "npm install",
  "pip3 install --upgrade pip setuptools wheel",
  "pip3 install --no-cache-dir -r requirements-core.txt",
  "pip3 install --no-cache-dir -r requirements-ml.txt || pip3 install --no-cache-dir tensorflow-cpu>=2.10.0 opencv-python-headless>=4.6.0"
]

[start]
cmd = "node server.js"

